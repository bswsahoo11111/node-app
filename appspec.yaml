version: 0.0
Resources:
 - TargetService:
     Type: AWS::ECS::Service
     Properties:
       TaskDefinition: ecs-task2
       LoadBalancerInfo:
         ContainerName: blue-green
         ContainerPort: 8080
